!!Activate your Python virtual environment!!
.venv\Scripts\Activate.ps1

Start docker desktop. 

# 1. Wipe out the old restricted cluster configuration
minikube delete

# 2. Fire up the fresh cluster with massive hardware headroom
minikube start --cpus=4 --memory=7000 --driver=docker

# 3. Bind your terminal shell context to Minikube's internal Docker registry
minikube docker-env | Invoke-Expression
docker ps

# 4. Install the Prometheus Operator core CRDs
kubectl apply --server-side -f https://raw.githubusercontent.com/prometheus-operator/prometheus-operator/main/bundle.yaml

@"
apiVersion: v1
kind: ServiceAccount
metadata:
  name: python-client-sa
  namespace: default
"@ | kubectl apply -f -

@"
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: python-client-sa-admin-binding
subjects:
- kind: ServiceAccount
  name: python-client-sa
  namespace: default
roleRef:
  kind: ClusterRole
  name: cluster-admin
  apiGroup: rbac.authorization.k8s.io
"@ | kubectl apply -f -

# 5. Build all 4 operational container layers
docker build  -t ml-app:latest -f ml-app/Dockerfile .
docker build  -t dispatcher:latest -f dispatcher/Dockerfile .
docker build  -t autoscaler:latest -f custom_autoscaler/Dockerfile .
docker build  -t load-tester:local -f load_tester_config/Dockerfile.tester .

# 6. Deploy your core applications, the database instance, and the autoscaler
kubectl apply -f manifests/ml-app-deployment.yaml
kubectl apply -f manifests/prometheus-rbac.yaml
kubectl apply -f manifests/dispatcher-deployment.yaml
kubectl apply -f manifests/prometheus-instance.yaml
kubectl apply -f manifests/custom-autoscaler-deployment.yaml
kubectl apply -f manifests/grafana.yaml
kubectl wait --for=condition=Ready pod -l app.kubernetes.io/name=prometheus-operator --timeout=120s

#check and make sure custom_autoscaler is deployed 
kubectl get deployment custom-autoscaler-deployment

# 7. Launch the Dispatcher Tunnel in a background window (dont close the windows)
# Spawn all 3 port-forwards into separate background processes
Start-Process powershell -ArgumentList "kubectl port-forward svc/dispatcher-service 8001:8001"
Start-Process powershell -ArgumentList "kubectl port-forward svc/prometheus-operated 9090:9090"
Start-Process powershell -ArgumentList "kubectl port-forward svc/grafana-service 3000:3000"

# Wait a brief moment for the network tunnels to establish
Start-Sleep -Seconds 3

# Clear terminal output and display access instructions cleanly
Clear-Host
Write-Host "Cluster Network Tunnels Established Successfully!" -ForegroundColor Green
Write-Host "--------------------------------------------------"`
Write-Host "Dispatcher App: http://localhost:8001"
Write-Host "Prometheus UI:  http://localhost:9090"
Write-Host "Grafana Server: http://localhost:3000  (Credentials: admin / admin)"
Write-Host "--------------------------------------------------"

# 8. Start Load Tester
kubectl delete job load-tester-job --ignore-not-found
kubectl apply -f manifests/load-tester-job.yaml

# 9. Dynamic status watch (Press Ctrl + C once all say 1/1 or 2/2 Running)
kubectl get pods -w
kubectl logs custom-autoscaler-deployment-7844667444-cqpkn -f

# 10. Logs 
kubectl logs -l job-name=load-tester-job -f
kubectl logs deployment/dispatcher-deployment --tail=20
kubectl logs deployment/ml-app-deployment --tail=30
kubectl logs deploy/custom-autoscaler-deployment --tail=30

# 11. Monitoring (Run directly in terminal)

"timestamp,replicas,p99_latency,queue_size" | Out-File -FilePath experiment_custom_autoscaler.csv -Encoding utf8

while ($true) {
    $Timestamp = [DateTimeOffset]::Now.ToUnixTimeSeconds()
    
    # 1. Fetch current active ML replica count
    $Replicas = (kubectl get deployment ml-app-deployment -o jsonpath='{.spec.replicas}')
    
    # 2. Query Prometheus for the TRUE 99th percentile dispatcher processing latency
    $LatResponse = Invoke-RestMethod -Uri "http://localhost:9090/api/v1/query?query=histogram_quantile(0.99,+sum(rate(dispatcher_response_time_seconds_bucket[1m]))+by+(le))"
    $Latency = "0.0"
    if ($null -ne $LatResponse.data.result -and $LatResponse.data.result.Count -gt 0) {
        $Latency = $LatResponse.data.result[0].value[1]
    }
    
    # 3. Query Prometheus for the real-time queue size gauge
    $QueueResponse = Invoke-RestMethod -Uri "http://localhost:9090/api/v1/query?query=dispatcher_queue_size"
    $Queue = "0"
    if ($null -ne $QueueResponse.data.result -and $QueueResponse.data.result.Count -gt 0) {
        $Queue = $QueueResponse.data.result[0].value[1]
    }
    
    # Output progress cleanly
    $LogLine = "$Timestamp,$Replicas,$Latency,$Queue"
    Write-Output $LogLine
    $LogLine | Out-File -FilePath experiment_custom_autoscaler.csv -Append -Encoding utf8
    
    Start-Sleep -Seconds 10
}

#FOR RERUN
docker build -t autoscaler:latest -f custom_autoscaler/Dockerfile .
kubectl rollout restart deployment custom-autoscaler-deployment
kubectl scale deployment ml-app-deployment --replicas=1


#GRAFANA

Install kube-state-metrics (needed for Replicas panel in Grafana)
kubectl apply -f https://raw.githubusercontent.com/kubernetes/kube-state-metrics/main/examples/standard/cluster-role.yaml
kubectl apply -f https://raw.githubusercontent.com/kubernetes/kube-state-metrics/main/examples/standard/cluster-role-binding.yaml
kubectl apply -f https://raw.githubusercontent.com/kubernetes/kube-state-metrics/main/examples/standard/service-account.yaml
kubectl apply -f https://raw.githubusercontent.com/kubernetes/kube-state-metrics/main/examples/standard/deployment.yaml
kubectl apply -f https://raw.githubusercontent.com/kubernetes/kube-state-metrics/main/examples/standard/service.yaml


@"
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kube-state-metrics
  namespace: default
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
    - default
  selector:
    matchLabels:
      app.kubernetes.io/name: kube-state-metrics
  endpoints:
  - port: http-metrics
"@ | kubectl apply -f -

IMPORT DATA SOURCE: http://prometheus-operated.default.svc:9090


# -------------------------------------------------------------------------
# 1. IMPORT the Autoscaler Comparison Dashboard
# -------------------------------------------------------------------------
{
  "id": null,
  "uid": "autoscaler-comparison",
  "title": "Autoscaler Comparison Dashboard",
  "tags": ["autoscaler", "latency", "kubernetes"],
  "timezone": "browser",
  "schemaVersion": 16,
  "refresh": "5s",
  "time": {"from": "now-30m", "to": "now"},
  "panels": [
    {
      "id": 1,
      "title": "99th Percentile Latency (P99) - SLO Target: 0.5s",
      "type": "timeseries",
      "gridPos": {"x": 0, "y": 0, "w": 24, "h": 10},
      "targets": [
        {
          "expr": "histogram_quantile(0.99, sum(rate(dispatcher_response_time_seconds_bucket[15s])) by (le))",
          "legendFormat": "P99 Latency",
          "refId": "A"
        },
        {
          "expr": "histogram_quantile(0.90, sum(rate(dispatcher_response_time_seconds_bucket[15s])) by (le))",
          "legendFormat": "P90 Latency",
          "refId": "B"
        },
        {
          "expr": "histogram_quantile(0.50, sum(rate(dispatcher_response_time_seconds_bucket[15s])) by (le))",
          "legendFormat": "P50 Latency",
          "refId": "C"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "s",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {"color": "green", "value": null},
              {"color": "yellow", "value": 0.3},
              {"color": "red", "value": 0.5}
            ]
          }
        }
      }
    },
    {
      "id": 2,
      "title": "ML App Replicas (CPU Cores in Use)",
      "type": "timeseries",
      "gridPos": {"x": 0, "y": 10, "w": 12, "h": 8},
      "targets": [
        {
          "expr": "kube_deployment_spec_replicas{deployment=\"ml-app-deployment\"}",
          "legendFormat": "Replicas",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "min": 0,
          "max": 6
        }
      }
    },
    {
      "id": 3,
      "title": "Dispatcher Queue Size",
      "type": "timeseries",
      "gridPos": {"x": 12, "y": 10, "w": 12, "h": 8},
      "targets": [
        {
          "expr": "dispatcher_queue_size",
          "legendFormat": "Queue Size",
          "refId": "A"
        }
      ]
    },
    {
      "id": 4,
      "title": "Average Latency",
      "type": "timeseries",
      "gridPos": {"x": 0, "y": 18, "w": 12, "h": 8},
      "targets": [
        {
          "expr": "sum(rate(dispatcher_response_time_seconds_sum[15s])) / sum(rate(dispatcher_response_time_seconds_count[15s]))",
          "legendFormat": "Avg Latency",
          "refId": "A"
        }
      ],
      "fieldConfig": {"defaults": {"unit": "s"}}
    },
    {
      "id": 5,
      "title": "Request Rate (req/s)",
      "type": "timeseries",
      "gridPos": {"x": 12, "y": 18, "w": 12, "h": 8},
      "targets": [
        {
          "expr": "sum(rate(dispatcher_requests_total[15s]))",
          "legendFormat": "Requests/sec",
          "refId": "A"
        }
      ],
      "fieldConfig": {"defaults": {"unit": "reqps"}}
    },
    {
      "id": 6,
      "title": "Live Stats",
      "type": "stat",
      "gridPos": {"x": 0, "y": 26, "w": 24, "h": 4},
      "targets": [
        {
          "expr": "histogram_quantile(0.99, sum(rate(dispatcher_response_time_seconds_bucket[1m])) by (le))",
          "legendFormat": "P99 Latency",
          "refId": "A"
        },
        {
          "expr": "kube_deployment_spec_replicas{deployment=\"ml-app-deployment\"}",
          "legendFormat": "Replicas",
          "refId": "B"
        },
        {
          "expr": "dispatcher_queue_size",
          "legendFormat": "Queue",
          "refId": "C"
        },
        {
          "expr": "sum(rate(dispatcher_requests_total[1m]))",
          "legendFormat": "Req/s",
          "refId": "D"
        }
      ]
    }
  ]
}


# ---- EXPERIMENT 2: HPA 70% CPU ----

# Stop custom autoscaler
kubectl scale deployment custom-autoscaler-deployment --replicas=0

# Reset ml-app to 1 replica
kubectl scale deployment ml-app-deployment --replicas=1

# Apply HPA 70%
kubectl apply -f manifests/hpa-70.yaml
kubectl get hpa -w
# Wait 30 seconds for HPA to initialize then Ctrl+C

# Run load tester
kubectl delete job load-tester-job --ignore-not-found
kubectl apply -f manifests/load-tester-job.yaml
kubectl logs -l job-name=load-tester-job -f
# Take Grafana screenshot when load test is running

# ---- EXPERIMENT 3: HPA 90% CPU ----

# Remove HPA 70%
kubectl delete hpa --all

# Reset ml-app to 1 replica
kubectl scale deployment ml-app-deployment --replicas=1

# Apply HPA 90%
kubectl apply -f manifests/hpa-90.yaml
kubectl get hpa -w
# Wait 30 seconds for HPA to initialize then Ctrl+C

# Run load tester
kubectl delete job load-tester-job --ignore-not-found
kubectl apply -f manifests/load-tester-job.yaml
kubectl logs -l job-name=load-tester-job -f
# Take Grafana screenshot when load test is running





Minikube Issues if improperly deleted or removed. 
# 1. Force remove the broken cluster profiles from memory
minikube delete --all --purge

# 2. Hard delete the hidden cached SSH/machine state files from your user profile directory
Remove-Item -Recurse -Force C:\Users\sumit\.minikube

# 3. Clean up any stuck network definitions hidden inside your local context
minikube update-context

# 4. kubectl delete deployments,jobs,statefulsets --all

#FOR RERUN
docker build -t autoscaler:latest -f custom_autoscaler/Dockerfile .
kubectl rollout restart deployment custom-autoscaler-deployment
kubectl scale deployment ml-app-deployment --replicas=1

docker build -t ml-app:latest -f ml-app/Dockerfile .
kubectl rollout restart deployment ml-app-deployment

docker build -t autoscaler:latest -f custom_autoscaler/Dockerfile .
kubectl rollout restart deployment custom-autoscaler-deployment
kubectl logs custom-autoscaler-deployment-84f8cb45d8-2fj67 -f | select -first 5

kubectl get pods -w


1. Average Latency (Seconds)
sum(rate(dispatcher_response_time_seconds_sum[15s])) / sum(rate(dispatcher_response_time_seconds_count[15s]))

2. 99th Percentile (P99​) Latency (The Project Requirement)
histogram_quantile(0.99, sum(rate(dispatcher_response_time_seconds_bucket[15s])) by (le))
